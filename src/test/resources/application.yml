spring:
  ldap:
    embedded:
      base-dn: dc=pivotal,dc=com
      credential:
        username: uid=admin
        password: secret
      ldif: classpath:test-schema.ldif
      port: 12345
      validation:
        enabled: false